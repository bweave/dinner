<div class="d-flex justify-content-end mb-3">
  <%= link_to "New menu", new_menu_path, class: "btn btn-primary" %>
</div>

<div id="menus">
  <% @menus.each do |menu| %>
    <%= render menu %>
  <% end %>
</div>
