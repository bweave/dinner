<div id="<%= dom_id recipe %>" class="card h-100">
  <% if recipe.picture.representable? %>
    <%= link_to recipe, target: "_top" do %>
      <%= recipe_header_img(recipe) %>
    <% end %>
  <% end %>
  <div class="card-body">
    <h5 class="card-title"><%= link_to recipe.name, recipe, target: "_top" %></h5>
  </div>
</div>
